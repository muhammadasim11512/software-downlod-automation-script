# software-downlod-automation-script
Skip the hassle of manual installs — this script sets up RabbitMQ, Redis, Keycloak, and JasperReports on any Linux server in a single command.  It automatically:  Downloads and installs each service  Configures them with safe defaults  Finds free ports if needed  Registers them as system services  Verifies everything is running  
